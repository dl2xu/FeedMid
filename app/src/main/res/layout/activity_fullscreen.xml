<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context="com.hackathon.feedmid.feedmid.FullscreenActivity"
    android:backgroundTint="#BD8D46"
    >


    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/grocery_budget"
        android:id="@+id/enterpricerangeTextView"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginTop="150dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColor="#F6E497" />

    <EditText
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/editText"
        android:background="#FFFFFF"
        android:layout_marginTop="5dp"
        android:layout_below="@+id/enterpricerangeTextView"
        android:layout_centerHorizontal="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        />

    <Button
        android:layout_width="179dp"
        android:layout_height="wrap_content"
        android:id="@+id/nextPageButton"
        android:layout_gravity="center_horizontal|bottom"
        android:background="@drawable/Feed_Me_Normal"
        android:layout_below="@+id/editText"
        android:layout_alignLeft="@+id/enterpricerangeTextView"
        android:layout_alignStart="@+id/enterpricerangeTextView"
        android:layout_marginTop="25dp" />


</RelativeLayout>
