<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context="com.hackathon.feedmid.feedmid.FullscreenActivity"
    android:backgroundTint="#39c3b3"
    >


    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->

    <Button
        android:layout_width="179dp"
        android:layout_height="wrap_content"
        android:text="Feed Me!"
        android:id="@+id/nextPageButton"
        android:layout_gravity="center_horizontal|bottom"
        android:background="#ee0000"
        android:layout_marginTop="75dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/enterpricerangeTextView" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Grocery Budget"
        android:id="@+id/enterpricerangeTextView"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginTop="150dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/editText"
        android:layout_below="@+id/enterpricerangeTextView"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
